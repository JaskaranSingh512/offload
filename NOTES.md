# NOTES — edit/overwrite freely, keep it short.

## What works
- Nothing yet. Repo has PRD.md only; app not scaffolded.

## What's next
- **Read EXECUTION_PLAN.md** — full start-to-finish build plan (stack, MCPs, schema, sprint sequence).
- Phase 0 FIRST: freeze the data contract (table/column names, post status model, content JSONB shapes) before any code.
- Then: Supabase project + MCP, scaffold Vite app, generate the 13 `src/*.jsx` (they don't exist yet).
- Wire `package.json` scripts: `lint`, `test`, `typecheck` (turns on verify.sh).

## Gotchas
- 4 channels only (Reddit, TikTok, Instagram, X). Video is founder-posted, never auto-published.
